# Environment variables (contains secrets - never commit)
.env

# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Seed-sweep artifacts (variance runs; the shipped checkpoint is transformer.pt)
models/transformer_seed*.pt
images/training_loss_seed*.png
